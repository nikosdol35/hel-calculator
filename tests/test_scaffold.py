"""Phase 1 scaffold test. Exists so CI has a green baseline before any
physics modules are implemented. Delete once real module tests exist
(per TESTING.md v1.1 §10 — every test must trace to a SPEC §3 case)."""


def test_scaffold_placeholder():
    assert True
