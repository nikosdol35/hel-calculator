[pytest]
# Custom markers — registered here so pytest does not warn about
# unknown marks. The validation suite is otherwise zero-config and
# uses sane defaults for everything else.
markers =
    e2e: end-to-end UI tests using Streamlit's AppTest framework. Slow (~30-90 s each); split into a dedicated CI job so they don't gate the physics-only suite.
